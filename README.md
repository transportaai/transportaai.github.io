## Local Development

### Windows
Run `cd .\docs; jekyll serve` to start the development server.

### First Time Setup
1. Install Ruby
2. Run `bundle install`
3. Start the server using the command above